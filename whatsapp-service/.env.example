# Conexión a la misma instancia de Postgres usada por Rails (retroai)
# En desarrollo local apunta a la base retroai_development creada por `bin/rails db:create`
DATABASE_URL=postgres://localhost:5432/retroai_development

# Puerto del servidor de health-check
PORT=3001
